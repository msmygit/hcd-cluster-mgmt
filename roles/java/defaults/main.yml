---
# Default variables for java role

java_version: "11"
java_package_debian: "openjdk-{{ java_version }}-jdk"
# Amazon Linux uses Corretto; RHEL/CentOS uses OpenJDK
java_package_redhat: "{{ 'java-11-amazon-corretto' if ansible_distribution == 'Amazon' else 'java-11-openjdk-devel' }}"